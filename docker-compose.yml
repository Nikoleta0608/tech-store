services:
  backend:
    build: ./backend
    ports:
      - "8080:80"
    depends_on:
      - db

  db:
    image: mysql:8
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: tech_db
      MYSQL_USER: user
      MYSQL_PASSWORD: password
